{
  "name": "production-orders-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
  "dev": "docker-compose up --build",
  "build": "yarn workspace @omnitest/shared-types build && yarn workspace backend build"
}
}
